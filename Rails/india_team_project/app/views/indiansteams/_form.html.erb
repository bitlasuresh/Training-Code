<%= form_with(model: indiansteam, local: true) do |form| %>
  <% if indiansteam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(indiansteam.errors.count, "error") %> prohibited this indiansteam from being saved:</h2>

      <ul>
        <% indiansteam.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
